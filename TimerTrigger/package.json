{
  "name": "timertrigger",
  "version": "1.0.0",
  "description": "The `TimerTrigger` makes it incredibly easy to have your functions executed on a schedule. This sample demonstrates a simple use case of calling your function every 5 minutes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^10.12.10",
    "@types/request": "^2.48.1",
    "github-api": "^3.0.0",
    "request": "^2.88.0"
  }
}
